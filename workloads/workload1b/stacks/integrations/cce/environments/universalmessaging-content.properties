#######################################
# Universal Messaging Content
#######################################

########## product specifics ##########

# run-time variable: server/cluster/docker
environment.type=${environment.type}

### The target hosts for the install
nodes=${target.nodes}

##UM content

# instance name
um.instance.name=${um.instance.name}           
um.jndi.cf.name1=${um.jndi.cf.name1}
um.jndi.cf.name1.url=${um.jndi.cf.name1.url}